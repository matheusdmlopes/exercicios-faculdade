strcpy(variavel, valor) // string copy

strcat(variavel, valor) // string concat - valor talvez seja possivel ser uma string

strcmp(variavel, valor) // string compare - valor talvez seja possivel ser uma string, validaçao é booleana, 0 = true, 1 = false.


fgets(variavel, tamanho, stdin) // stdin = standard input = teclado


estruturas em C

typedef struct {
	char[10] nome;
	} nomeDaEstrutura;
	
nomeDaEstrutura nomeDaVariavel;

TFunc f1;
f1.nome;


	
